select p.product_id, coalesce(round((sum((units * price)) * 1.0) / (sum(units) * 1.0), 2), 0) as average_price
from prices as p
full outer join unitssold as u
on u.product_id = p.product_id and u.purchase_date >= p.start_date and u.purchase_date <= p.end_date
group by p.product_id
